# --- Core AI Framework ---
# Downgraded to 2.2.2 — torch 2.3.1 is not available on macOS via PyPI
torch==2.2.2
torchaudio==2.2.2
torchvision==0.17.2

# --- The NumPy 2.0 Fix ---
# Must be <2.0. NumPy 2.0 fundamentally breaks DeepFace/SciPy/TensorFlow.
numpy<2

# --- Hugging Face (Text & Audio Wrappers) ---
# Locked to 4.38.0, was breaking with 5.3.0 due to it disabling Torch if the Torch version was below 2.4
transformers==4.38.0

# --- Audio Processing & Recording ---
# llvmlite 0.47+ has no pre-built wheel for macOS x86_64; pin to latest available binary
llvmlite==0.45.1
numba==0.62.1
librosa>=0.10.1
sounddevice>=0.4.6
soundfile>=0.12.1

# --- Video Processing & Facial Emotion ---
deepface
tf-keras
opencv-python

# --- TTS (Text-to-Speech) ---
# 0.14.1 requires Python <3.11; using latest compatible version
TTS==0.22.0
